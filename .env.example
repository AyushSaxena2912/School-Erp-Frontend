# Frappe backend site & API URL configuration
# Local Vite: leave VITE_API_URL empty so /api is proxied (no CORS)
# Direct (Docker/prod): set full URL e.g. http://127.0.0.1:8000
VITE_API_URL=
VITE_FRAPPE_SITE=education.local
