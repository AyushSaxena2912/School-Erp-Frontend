services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    environment:
      
      VITE_API_URL: ${VITE_API_URL:-http://host.docker.internal:8000}
    volumes:
      - .:/app
      - /app/node_modules
    extra_hosts:
      - "host.docker.internal:host-gateway"
